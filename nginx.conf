user ec2-user;
events {}
http {
        server {
                listen 80;
                include /etc/nginx/mime.types;
		location / {
			root /home/ec2-user/vt-project-combined/frontend;
		}
                location /api {
                        proxy_pass http://localhost:5000/api;
                }
        }
}